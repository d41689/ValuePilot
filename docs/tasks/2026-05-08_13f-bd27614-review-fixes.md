# 13F bd27614 Review Fixes

## Goal / Acceptance Criteria

Address accepted verifier findings for commit `bd27614`:

- Block direct `retry-cik-search` calls for confirmed managers so confirmed CIK changes must use the revoke audit path.
- Restore quarter health priority so `setup_required` outranks revoked-CIK `needs_review`.
- Reduce quarter detail filing status/holding count N+1 behavior.
- Preserve the legacy top-level `filings` response semantics while keeping paged `filings_page`.
- Protect EDGAR pause-state reads/writes with the existing request-event lock.
- Tighten tests for reconfirmed quarter health and EDGAR request ledger accounting.

## Scope

In:

- Backend service/API behavior and unit tests.
- Product plan wording if needed for accuracy.
- Docker verification.

Out:

- New schema migrations.
- Frontend feature changes unless required by response contract.

## Files To Change

- `backend/app/services/thirteenf_admin_dashboard.py`
- `backend/app/edgar/client.py`
- `backend/tests/unit/test_13f_admin_dashboard.py`
- `docs/tasks/2026-05-08_13f-bd27614-review-fixes.md`

## Test Plan

- `docker compose exec api pytest -q tests/unit/test_13f_admin_dashboard.py -q`
- `docker compose exec api pytest -q`
- `docker compose exec web node --test lib`
- `docker compose exec web npm run lint`
- `docker compose exec web npm run build`

## Progress Notes

- 2026-05-08: Created task log before code changes.
- 2026-05-08: Added regression tests for confirmed-manager retry guard, setup-required health priority, reconfirmed quarter health recovery, legacy full `filings` response semantics, and EDGAR request ledger counting.
- 2026-05-08: Implemented accepted fixes:
  - `retry-cik-search` rejects confirmed managers and points callers to the revoke path;
  - `_quarter_health` checks `setup_required` before revoked-CIK `needs_review`;
  - quarter detail precomputes holding counts/statuses once and reuses them across counts, filters, page rows, and pending/failed previews;
  - top-level `filings` remains the complete legacy list while `filings_page.items` is the paged slice;
  - EDGAR pause state is read/written while holding the request-event lock.

## Verification

- `docker compose exec api pytest -q tests/unit/test_13f_admin_dashboard.py -q` — passed, 49 tests.
- `docker compose exec api pytest -q` — passed, 288 tests.
- `docker compose exec web node --test lib` — passed, 105 tests.
- `docker compose exec web npm run lint` — passed.
- `docker compose exec web npm run build` — passed.
