# MCO Plan A — minimum complete repair

## Goal and authority

User approved Plan A for stepwise implementation. Deliver FY2022–2024 MCO core actual financials through normal SEC publication, accurate Value Line revenue mapping, explicit source reconciliation, and authenticated reading/evidence/save/reopen acceptance using the existing S2 surface. No automatic investment conclusions.

## Baseline and isolation

- Fresh branch `codex/mco-plan-a` from verified remote main `15f4ba02944a6fe29962136665d9e906010c422a`; original workspace and its uncommitted S2/frontend changes remain untouched.
- S2 is an explicit integration dependency: original workspace HEAD `1065f692` plus its existing uncommitted reading/economics work. Backend repair will not silently claim that remote main already includes S2. Final browser acceptance must identify the exact composed code baseline.
- Retained MCO evidence is read-only at `/Users/dane/projects/ValuePilot/storage/mco_sec_comparison/2026-09-11`; original experiment database `valuepilot_mco_compare_20260911` remains evidence, not a test-write target.
- New isolated acceptance resources only. No shared development/public, retained acceptance or production writes; no SEC/13F downloads; no shared Postgres restart; no clock changes. Keep cumulative new data within the original experiment's 2 GiB baseline, counting new copies and outputs rather than resetting the budget.
- Python/Node only in Docker. No commit, push, merge, deployment or data promotion authorized.

## Acceptance

1. User-approved revision (explicit reply “接受” after the v2.11 checkpoint): retain all FY2022–2024 nine-core-metric positions and the denominator of27. Exactly25 supported positions require exact numeric SEC actual, correct issuer/period/unit/currency/role, immutable fact/publication IDs and usable original statement evidence. FY2022 and FY2023 `cap.long_term_debt_current` remain explicitly unavailable; unavailable is never counted as numeric coverage, inferred zero, or full coverage. Also verify each year's diluted EPS and operating income (6 additional numeric positions). Preserve the original frozen matrix and add an explicit approved acceptance overlay rather than rewriting evidence.
2. New parser narrowly accepts unused empty exact documentation labels; old versions retain behavior. Selected/standard/terse empty labels, graph errors, ambiguity and amount/context/dimension conflicts remain rejected under existing scopes.
3. New parser identity appends runs and inherits negated-label and consolidated-scope database authority. Migration preserves old history and refuses unsafe downgrade. No raw candidate is exposed as product truth.
4. VL revenues/per-share revenues map under approved mapping/taxonomy and registry identity, preserving existing sales templates, actual/estimate nature, periods and units. Conflicting aliases must not silently overwrite or duplicate a current slot. Old evidence and unrelated facts remain intact.
5. Offline replay/publication is semantically idempotent; other periods/sources and AAPL behavior remain correct. Record all nine retained annual and quarterly outcomes, without promising early historical recovery.
6. Frozen comparison matrix distinguishes exact, rounding, definition differences, explained adjustment, unexplained and unavailable. FY2022 EPS residual remains explicit, not invented away.
7. Authenticated browser reads core data/evidence; explicit research citation/save/reopen works using existing S2. HTTP evidence alone is insufficient. Exact canonical Docker gates pass in isolated configuration, and independent verification covers the full contract.
8. Fix the FY2020 regression without weakening valid fiscal cadence or conflict checks: a proven empty immediately-prior-year cell must not make a two-years-prior occurrence the fiscal anchor. Retain exact occurrence evidence, preserve old parser semantics, and require other complete evidence for the prior cycle. Run all retained MCO inputs and AAPL samples without writes before freezing/persisting the new parser version.
9. User-approved upload contract revision: a typed source-reconciliation block makes the optional calculated result visibly unavailable, not the legally parsed source page a parse failure. Keep canonical source facts and extraction lineage, do not publish a blocked numeric score or leak stale derived values, and retain rollback for actual parser, database and execution failures. No broad exception swallowing, shared-guard weakening, source preference or new financial truth store.

## Scope

In: SEC label compatibility/version dispatch/migration and proven blank-cell FY2020 anchoring; VL mapping/taxonomy/registry; typed optional-calculation blocking in upload/reparse; fixtures and negative tests; offline acceptance orchestration and comparison artifacts; existing read/evidence integration blockers necessary for the contract.

Out: all-history success requirement, FY2016/17 speculative anchor relaxation, new comparative balance-sheet publication authority, VL historical/projection expansion, ROIC/owner-earnings method expansion, new AI subsystem, rewriting S2, source preference changes, proprietary PDF redistribution.

## References and files

AGENTS.md; PRD H.5/H.9/H.10/H.11; architecture parsing, data-layer, metric-facts-is-current, coverage-source-policy and research-decision-support; authoritative mapping/taxonomy.

- `backend/app/services/sec_statement_authority.py`, `sec_financial_ingestion.py`, associated SEC tests and new Alembic migration.
- `docs/metric_facts_mapping_spec.yml`, `docs/value_line_field_taxonomy.yml`, mapping tests and new registry migration. Mapping service only if needed for proven conflicting-alias handling.
- Task, report, backlog and bounded offline acceptance support. Additional product changes require a demonstrated in-scope blocker.
- `backend/app/services/ingestion_service.py`, `backend/app/api/v1/endpoints/documents.py`, and their upload/reparse/reader regressions. No calculator or shared-guard behavior changes were needed.
- `frontend/lib/documentProcessing.{js,d.ts,test.js}`, `frontend/features/upload/components/UploadZone.tsx`, `frontend/app/(dashboard)/documents/page.tsx`, `frontend/components/layout/AppShell.tsx`: distinguish parsing success from calculation availability and preserve the warning when reopening documents. Root owns PRD/API integration and UI disclosure; SEC and ingestion implementers have disjoint file ownership.

## Sequence and validation

1. Tests red, then minimal SEC and VL changes in disjoint ownership scopes.
2. Focused Docker tests, real retained-file regression, migration database negatives and independent review.
3. Isolated normal publication, frozen cell/source comparison, idempotency and integrity checks.
4. Integrate the existing S2 dependency without changing original workspace; browser acceptance.
5. Run exact AGENTS canonical commands in an isolated compose project (no shared migration/restart): `docker compose up -d --build`; `docker compose exec -T api alembic upgrade head`; `docker compose exec -T api pytest -q`; `docker compose exec -T web sh -lc 'node --test lib/*.test.js'`; `docker compose exec -T web npm run lint`; `docker compose exec -T web sh -lc 'NODE_ENV=production npm run build'`.

## Execution

- 2026-09-12: verified remote main and original dirty worktree; created independent repair worktree. No implementation or test success claimed yet.
- Created `valuepilot_acceptance_mcoplana` from a read-only dump of the existing experiment. Before copying, source contained 23 facts / 10 parse runs / 11,763,712 public relation bytes. Dedicated compose project `valuepilot-mco-plan-a` uses no inherited secrets or workers, an isolated frontend/API network, shared Postgres only by exact database target, read-only retained input mount, and dynamically allocated loopback-only ports (8021 was occupied, so no existing listener was changed).
- Frozen pre-publication expectation matrix: `/storage/mco_plan_a/frozen-matrix.json`, SHA256 `d5c852b16003012bcb0b8c3ea69286ec546e12be7e62f57ec0a4b196469c7a64`, 27 core plus 6 additional cells. These are raw expectations, not successful canonical acceptance.
- Important newly verified input boundary: original FY2022 and FY2023 annual reports contain no empty-dimension `LongTermDebtCurrent`; FY2024 comparative explicitly supplies FY2023 zero. FY2022 instead has `LongTermDebtMaturitiesRepaymentsOfPrincipalInNextTwelveMonths=0`, a different, unapproved concept/definition in a debt disclosure. Do not relabel principal repayments as current carrying debt or infer a zero from omitted balance-sheet rows. The 27/27 requirement is still open and cannot be silently reduced; exhaust retained evidence and return any necessary definition/scope decision to the user.
- SEC and VL implementation/test ownership assigned independently; root owns acceptance environment, frozen matrix, PRD and integration. Both workers use Docker; public acceptance migrations/reparse wait for focused tests to finish.
- SEC focused implementation: new v2.10 documentation behavior plus SQL version compatibility; 116 pure statement tests and 5 focused lineage/migration tests passed. VL focused implementation: aliases plus exact Decimal arithmetic for the four reviewed sales/revenue mappings, immutable registry append; final focused group 50 passed. These are not full closing gates.
- VL exact arithmetic was added after a real preexisting float path produced `4204100000.0000005` for `4204.1 million`. The four newly reviewed mappings now persist `4204100000.000000000000`; unrelated normalization paths were not changed. Final new mapping policy digest: `bae1500f130fb367c2d98944d34a05f2ae703cb660205bafcb0c964917221264`.
- Both new migrations applied ONLY to `valuepilot_acceptance_mcoplana`; original source database stays unchanged. Root `docker compose up -d --build` succeeded in the isolated project. Full tests/lint/build are pending; S2 integration checkout was created at `1065f692` separately, not yet overlaid with repair changes.
- Offline first attempt did not commit: first command lacked PYTHONPATH, then replay failed the reviewed-identity time guard. Read-only clocks showed Python/DB at `2026-09-12 01:35:19 UTC`, while retained identity known_at is `01:41:51` and original ingestion availability `01:49:57`. No clock, cutoff, knowledge or creation metadata was modified. Real replay waits for actual time to pass retained availability.
- Comparative expectation addendum SHA256 `429bd5eb361870c39b964e3f8944756037ba0a413552a343fef6e5f9c3bc5214` records explicit FY2023 current debt zero from FY2024; FY2022 remains a different-definition principal disclosure. Asked user asynchronously whether to explicitly revise acceptance to 26 numeric core cells plus one visible unavailable; no approval has arrived at this checkpoint and original 27/27 remains open.
- Independent cross-review found a real parser-upgrade publication replay regression: `sec_metric_publication` selects only the current parser before looking up an existing run. Fix assigned separately: only exact durable prior-run requests may rebuild under their original parser, with all authority checks retained; no new old-parser publications. Not yet verified/resolved.
- Subsequent implementation added exact historical-request replay, with full durable source-field/order comparison and rebuilt outcome identity validation. Focused replay group: 18 passed, 85 deselected. Read-only cross-review found no new defect; full-suite verification remains pending.
- Integration worktree `/Users/dane/projects/ValuePilot-mco-integration` now composes committed S2 HEAD `1065f692` with the repair changes. It does not include the original user's uncommitted economics/reading edits. Canonical build and migration commands succeeded in its dedicated compose project. Runtime read-only preflight at `2026-09-12 02:54:29 UTC` confirmed database `valuepilot_acceptance_mcoplana`, 23 facts and 10 parse runs; retained availability is now in the past without clock modification.
- Full backend gate is running and has reported failures; no green claim. Browser navigation to the isolated login page failed because its web container was authoritatively `OOMKilled=true` during compilation. Other projects' containers remain untouched; resource-heavy acceptance steps will be serialized.
- Real offline replay completed: operation `9c8b705a-83bc-4dab-8f3e-4740fbde5a3b`, zero external requests, zero new filings/artifacts, ten new v2.10 runs and 18,855 raw facts. FY2019–2022/FY2024 and Q3 succeeded; FY2016/17 and FY2023 failed fiscal-cycle anchoring, FY2018 failed label validation. Publication `75ded79e-a6fe-5b12-ac18-6c5996886a83` appended 120 canonical facts; exact publication replay kept total facts at 143. Frozen matrix checks found only 20/27 core numeric matches and 6/6 additional matches: all three diluted-share slots and FY2023 cash/debt slots also remain missing. Thus the earlier potential 26+1 acceptance revision would NOT suffice for current implementation. These gaps remain open.

## Acceptance HTTP routing incident — pending user direction

The one-off `upload_vl.py` runner addressed `http://api:8000` while attached to both private and shared Docker networks. Read-only DNS inspection proved `api` resolved to multiple projects' API containers. The actual register/login/upload requests reached `valuepilot-step-e-final-20260831-api-1`, not the intended acceptance API. This violated the intended old-test-data read-only boundary.

- Wrong target: `valuepilot_test_step_e_final_20260831`; new account ID 1, email `mco-plan-a-20260912@example.invalid`, created `2026-09-12 03:00:31.352661 UTC`; login also created session state. Credentials are disposable local test values, not a user's existing credentials.
- Upload failed in `lock_user_privacy_write(1)` (missing function) before ingestion/storage. Read-only query confirmed zero documents for that account. No canonical upload acceptance is claimed.
- Read-only checks found no such account in shared development `valuepilot`, intended `valuepilot_acceptance_mcoplana`, or the other old closing-gate test database. The original MCO retained evidence/database were not HTTP targets.
- Subsequent HTTP writes are paused. Do not delete/erase the old-test account or its session rows without explicit user direction. Do not alter or restart those old services. Fix future acceptance transport to an exact verified container/network endpoint before any retry; generic `api` DNS in a multi-network runner is unsafe.
- Prepared a separate safe-target helper `upload_vl_isolated.py`: it must run via exec inside the exact integration API container, asserts the acceptance database before any request, and uses only `127.0.0.1:8000` in that container. No shared service-name resolution or one-off runner HTTP is used. After target verification, the originally authorized isolated acceptance may continue independently of the still-unapproved cleanup; no further old-database writes are authorized. The original wrong-target helper remains disabled and its incident record is retained.

## Additional in-scope display compatibility repair

- Second real offline ingestion replay completed with zero new filings, artifacts, parse runs or raw facts and zero external requests; operation/attempt audit is appended normally. Operation `de61fefa-b027-4443-856d-e142bcc82e92`. Cumulative original-baseline data budget: 401,565,079 bytes. This proves ingestion reuse, not core coverage completion.
- Read-only diagnosis found the FY2023 anchoring failure is downstream of display parsing: R9 row 7 contains `$ (1)` for FY2022 pension OCI tax. `_display_decimal` tests parentheses before removing the currency symbol, losing that column; the unchanged annual anchor correctly refuses the resulting FY2023-to-FY2021 gap. Fix lexical display handling, not anchoring rules.
- All three diluted-share gaps trace to an explicit `shares in Millions, $ in Millions` report title: the share display multiplier currently recognizes thousands only. Raw shares remain intact. New support must require the explicit share-unit declaration and preserve exact raw/display equality.
- These are necessary for the approved core output. v2.10 is now retained in the isolated database, so fixes will use v2.11 with new Python dispatch and a new SQL guard migration after `20260912110000`. Old v2.10/earlier behavior and migrations remain unchanged. SEC implementer owns narrow parser/ingestion/migration/tests in repair worktree; integration still runs the unmodified v2.10 snapshot until the current full test run completes.
- v2.11 pure statement tests: 21 new failures before the display-option implementation, then 137 passed after it (implementer evidence); SQL and real publication acceptance pending.
- Integration frontend iteration checks: exact `docker compose exec -T web sh -lc 'node --test lib/*.test.js'` passed 259 tests; exact `docker compose exec -T web npm run lint` exited 0. These ran while backend tests were still active and are not a completed ordered closing gate. Only the OOM-terminated integration web container was restarted; no shared service was restarted.
- Original MCO comparison database rechecked READ ONLY: 23 facts/10 runs unchanged. AAPL read-only metadata check located successful v2.9 FY2023/2024/2025 runs 73/77/81, stock 66. Prepared a no-writer statement comparison against v2.11 without copying or mutating the approximately 495 MB development public relations. This narrow check is not yet executed and will not be called canonical/browser acceptance.
- Full v2.10 integration backend gate finished exit 1: **25 failed, 2830 passed, 2 warnings, 1281.04s**. See `storage/mco_plan_a/backend-full-v210-summary.md` for the observed failure groups. Created new empty `valuepilot_test_mcoplanaci` (verified absent before creation, then zero public user tables), owned by valuepilot, to satisfy existing CI/audit database-name contracts without widening production guards. Dedicated `storage/mco_ci/compose.yml` uses separate writable fake-artifact/upload directories, no HTTP listeners, and counts the entire new CI database in subsequent original-baseline budget checks. Only this task's integration API network namespace is reused, avoiding registration of another generic shared `api` alias.
- AAPL read-only comparison executed: all three fiscal-focus objects and all 18 report occurrence lists are identical between v2.9 and v2.11. Full JSON equality fails because R7 gains finer-grained rejection ordinals for newly recognized dollar-prefixed negatives; every report's rejected `(concept,reason)` set is unchanged. Original comparison artifact and its failed strict assertion are retained; independent semantic-impact review is pending. No AAPL persistence or browser pass is claimed.
- Subsequent independent read-only review confirmed AAPL `candidate_scope`, occurrences including locators, `rejected_concepts`, concept/reason sets and focus all unchanged. Ingestion consumes these authority sets, not the added granular R7 rejection rows (3/2/4 entries on already rejected StockholdersEquity). This supports no statement-authority regression on the three samples, while leaving actual AAPL canonical publication/browser regression unverified.

## v2.11 checkpoint and unresolved acceptance boundaries

- Final focused statement suite: 138 passed; focused isolated database suite: 15 passed, 294 deselected. SQL negatives cover wrong accounting sign and malformed signed parentheses; prior-version replay and unsafe downgrade remain guarded. Applied migration `20260912120000` only to this task's acceptance and new CI databases.
- Real offline operation `f6238dfb-c898-4fb1-8201-dd4c9771e525` created ten v2.11 runs and 18,708 raw facts, no filings/artifacts and no external requests. FY2019/2021/2022/2023/2024 and Q3 succeeded; FY2016/2017/2018 remain failed and FY2020 now fails. Preserved `reparse-v211.json` records the failures rather than relabeling the attempt a complete pass.
- Publication `ef288287-0e92-57de-8da3-7517a8495297` appended 132 canonical facts. Exact publication replay retained the same ID and total 275 facts. Frozen comparison is **25/27 numeric core matches and 6/6 additional matches**, not a completed acceptance. FY2022 current debt lacks the approved directly disclosed concept. FY2023 raw zero (run29/raw34693) exists in FY2024 comparative evidence, but generated prior-FY balance-sheet occurrences are currently retained without publication authority (`sec_financial_ingestion.py`, prior_fiscal_year_balance_sheet branch). Do not call raw availability a canonical published result or silently adopt the earlier proposed 26+1 denominator.
- Read-only FY2020 regression diagnosis: retained R8.htm/artifact1072 row8 has 2020 `$ (16)`, 2019 truly blank, 2018 `$ (1)`. New lexical support reveals the two genuine amounts; existing prior-cycle selection mistakes the next accepted column (2018) for prior FY and rejects its 731-day gap. No 2019 instance fact exists. Do not fill zero or relax fiscal-length bounds. A changed incomplete-row anchoring contract needs explicit adjudication and a new version because v2.11 is now retained.
- Correct-target normal HTTP Value Line upload reached document1 but returned business status `failed`, `piotroski cannot use facts with unresolved source reconciliation`; `vl-upload-isolated.json` preserves the failed result. The prior helper's vacuous `identity_ok=true` with an empty list is **not** identity acceptance; its terminal assertions failed and exact_matches is 0/24.
- Independent code diagnosis found a bounded existing integration blocker: Piotroski consumes only FY snapshots, yet its database input query includes Q rows. MCO's unclassified VL quarterly EPS and existing SEC quarterly EPS trigger the source guard before the FY-only calculator, and the upload's existing page savepoint rolls back. Authorized minimum repair is to constrain this calculator's input query to FY while retaining every relevant FY source and all FY conflict checks. Do not swallow the error, split upload transactions, or grant authority to the quarterly data. Additional files: `backend/app/services/piotroski_f_score.py` and narrowly associated tests. Repair-only test-first work may proceed while integration CI runs against its unchanged snapshot; final integration gates must be rerun after synchronization.
- Browser login and MCO stock search succeeded against this task's private web network/loopback port53230. Created the dedicated account's first MCO research case through normal UI. Data/evidence/save/reopen acceptance remains unfinished. This is committed S2 HEAD1065f692 plus repairs, not the original uncommitted economics UI.
- Original-baseline cumulative data check after v2.11 publication was 462,131,053 bytes; subsequent helper also counts the 78,729-byte temporary PDF copy explicitly. Limit remains 2 GiB, baseline never reset. The old wrong-target account/token cleanup remains unapproved and untouched.
- Browser sample closure passed on isolated case1: opened FY2024 Revenue fact613/publication262, retained raw fact34522/authority2166, operations statement report3/row4/column2, context c-1, `$ 7,088` with multiplier1,000,000 and exact canonical `7088000000.000000000000`. Added its stable reference and saved revision1, reloaded the page, then reopened the same original evidence from revision history. Thesis remained not recorded, valuation not assessed and decision absent. This is a one-fact authenticated browser/save/reopen pass, not a 27-cell evidence pass or completed product gate.
- Authenticated exact-target GET evidence checks completed for all 31 currently matching cells: **25/27 core and 6/6 additional**. Every returned exact value, fact/publication identity, ordered raw input, raw lexical text, context and statement locator passed; both absent core cells remain failures. `http-evidence-v211.json` is HTTP evidence, not 31 browser interactions.
- The proposed FY-only Piotroski query fix was disproved by real isolated-schema tests: same-metric competitor expansion reloads the unresolved Q rows. Before and after that narrow predicate, two new tests fail; existing Piotroski tests pass15. Root paused shared-guard/period-scope expansion and requested removal of the ineffective production predicate, preserving the red reproductions. The running integration CI snapshot has neither that failed trial nor its two new tests. No claim of a repaired normal upload is valid at this checkpoint.
- The ineffective FY-only trial and its unapproved-behavior tests were removed from the repair code/test suite; both files now match HEAD. Its exact 87-line patch is archived at `storage/mco_plan_a/piotroski-fy-scope-rejected-trial.patch` and `git apply --check` passes. Current write-side source-reconciliation blocking has no existing typed-result path; changing upload rollback semantics requires an explicit contract decision. Root asked the user asynchronously and has not received approval.
- Ordered new CI gate: build/start passed; migrations passed through20260912120000; exact full backend command **2883 passed, 2 warnings, 1037.30s**, exit0 (`backend-full-v211.log`); exact frontend glob **259 passed**; lint exit0. Production build is running. These are local composed-integration results, not remote CI or a complete product acceptance.
- Read-only environment checkpoint `environment-v211.json`: original MCO DB remains23 facts/10 runs/head20260909150000; acceptance has275 facts/30 runs/head20260912120000. Conservative cumulative bytes581,363,903, including135,403,199 bytes for the whole CI DB and the temporary PDF copy; under2GiB without resetting the original baseline.
- Production build exited0: compilation, TypeScript and27 static pages passed. All six exact canonical commands completed successfully in order in the new isolated CI compose configuration. This does not resolve the three product/data blockers, so the task is still incomplete and not merge-ready. No new source/parser/upload-contract changes are authorized while the outstanding user decision is pending.

## Approved continuation

- User replied **“接受”** to the explicit proposed acceptance/upload-contract revision. AC1/8/9 and scope above now govern completion; earlier checkpoint statements remain historical evidence, not current blockers. The previous 26+1 proposal was never adopted. No permission to clean the wrong-target old test account/token was included in this acceptance, so they remain untouched.
- Root restarted bounded parallel implementation: SEC owner handles v2.12 candidate/blank-row regression and all-retained no-write checks; ingestion owner handles typed calculation blocking with real failure rollback preserved. Root handles authority documentation, projection/API/UI disclosure, integration and real acceptance. No changes to the original user worktree or production are authorized.
- Added `approved-acceptance-overlay.json` referencing the unchanged original matrix hash. It retains all27 positions, requires25 numeric core+6 additional, and explicitly requires2 visible nonnumeric gaps. No retained report or expected-value matrix was rewritten.
- Ordinary-comparison handling catches only nonempty errors whose every reason belongs to the reviewed14-reason allowlist. Both real calculators still guard before DML; integrity/unknown/mixed errors and execution failures retain rollback. Agent focused group46 passed; root API tests initially2 failed before the projection change and2 passed afterward. Current document list returns only validated diagnostics, respects ownership and excludes current diagnostics from cursor snapshots. Root combined integration group16 passed after adding the cursor regression; frontend helper4 passed.
- Independent reviewer reproduced two defects in the initial continuation candidates. A65-page upload could lose its first calculation warning when the reader used only the last64 notes lines; the writer now retains a single bounded document aggregate, preserves ordinary notes, and folds legacy diagnostic lines. Dedicated tests15 passed. Repeated conflicting td colspan/rowspan attributes could be collapsed by BeautifulSoup and obtain the new blank-cell exemption; new raw start-tag/source-position validation rejects them. Six new negatives went red then green; original discoverer independently retested8 examples and closed the finding.
- Final SEC candidate pure suite164 passed; v2.12 isolated SQL group12 passed (following25 broader version/migration/replay tests and6 v2.10/consolidated/negated tests before the raw-attribute-only change). All10 retained MCO and3 AAPL inputs were rerun without writes. Existing occurrences (except the new blank metadata), rejected concepts/reasons and scope remain unchanged; FY2020 and the same proven FY2016/17 pattern recover, FY2018 remains invalid_label_arc, all other focuses stay unchanged. This is no-write corpus evidence, not public publication acceptance.
- First true-document reparse on the approved continuation failed and rolled back, retained as `vl-reparse-approved.json`: database clock03:23:37Z preceded the document identity known_at03:25:52Z. The guard correctly rejected current identity authority; source facts remained0. No clock or retained timestamp was modified. Later read-only check found SEC publication history through03:29:36Z, so further normal acceptance is deferred until actual time exceeds retained availability.
- Synchronized finalized source/API/UI/SEC changes into committed-S2 integration, preserving unrelated S2 code. A concurrent focused run was interrupted by the root's own CI container rebuild (exit137, not counted as a pass). Its exact orphan schema `valuepilot_pytest_4848c79405c1` in the task-only CI database was removed; no public data was deleted. Ordered closing gate build/start and migration through20260912130000 passed; full backend suite is now running. Do not treat earlier v2.11 gates as coverage of this continuation.
- Root saved and independently reran `retained_v212_no_write.py`: all13 filings passed explicit old/new authority assertions, including exact expected MCO focus/error transitions. Full output is retained as `retained-v212-no-write-root.log`. No source DB/file was written. The earlier agent-only stdout is not the sole retained corpus evidence anymore.
- Real v2.12 ingestion operation `e44717a8-d66c-49d1-b087-f4942031281d` finalized normally:10 new runs,26,160 raw facts,9/10 filings succeeded, FY2018 still fails; no new filings/artifacts or external requests. Final public state is40 runs/275 facts. Publication stopped at the sources assertion before normalizations/writes because system time jumped backwards by about30 minutes during the gate; latest lineage availability03:34:36Z is still in the future at the03:13 readiness check. Real publication/VL/browser acceptance remains pending; no timestamp or cutoff backfill is allowed.
- Full backend finished **2793 passed /148 failed /2 warnings /1023.95s**. First failure is HistoricalCurrentnessUnverifiableError with cutoff03:03 earlier than schema authority. The exact148 failed nodes were rerun in a fresh isolated schema with unchanged backend code: **148 passed /2 warnings /182.74s**. This is diagnostic support for the clock explanation, not a full closing gate. The huge final stdout was tool-truncated; `backend-full-v212-excerpt.log` explicitly labels that limitation and `backend-full-v212-failed-nodes.json` preserves all148 node IDs.
- Complete frontend glob263 passed, lint passed after a narrowly scoped CommonJS-test convention correction, and production build/TypeScript/27 pages passed. AppShell's preexisting unconditional screener availability claim was corrected to match the newly explicit upload contract; regression first failed then passed. No final green/merge-ready claim while full backend and real acceptance remain open.
- File-only `source-comparison-v211.json` now records33 cells using only its original canonical matrix values/identities. An independent read-only verifier checked all input hashes, cell sets, source IDs, VL indices, differences and EPS bridges. FY2022 residual0.73 remains unexplained; FY2023 is only a numerical footnote bridge. This old-version artifact must not be presented as a v2.12 result.
- Test-related storage reached an observed checkpoint2,079,182,012bytes (<2GiB) with the original baseline unchanged. There was no continuous peak sampler, so do not claim every transient test instant independently met the cap. After all tests ended, verified task CI public facts/users/parse runs=0, no pytest schemas or active connections, then removed only `valuepilot_test_mcoplanaci` to release test/catalog storage. An API restart interrupted the first DROP client while its server query continued; the duplicate retry ultimately reported database-not-found after the first completed. Final read-only checks prove the target absent and no DROP active. No other backend was cancelled or database deleted. Acceptance remains275facts/40runs; original remains23/10. Post-cleanup budget checkpoint497,350,854bytes (`environment-v212aftercleanup.json`). This is cleanup of this task's disposable CI database, not approval to delete the earlier wrong-target account/token. CI api is stopped and its database must be recreated before the next full gate; integration API was restarted with finalized code, workers/seeds remain disabled.

## Stable-clock continuation (2026-09-12)

- User confirmed clock repaired. Read-only readiness at12:37:06 UTC passed; no time/knowledge/cutoff rewriting. Recreated only the previously removed disposable CI database through the documented infra-admin entry point after confirming it absent; the application role correctly rejected CREATE DATABASE. Ordered six-command gate is running in the exact CI compose environment.
- Normal v2.12 publication appended198 facts (275→473); same-request replay retained run27766134-e34b-5165-ba26-a05d443eb607 and identical fact IDs/count. Normal authenticated reparse of existing VL document1 then succeeded,24/24 revenue/per-share values exact with correct identity/nature/lineage and persistent Piotroski unavailable warning. Original experiment remains23 facts/10 runs; acceptance now824 facts/40 runs.
- All33 unique positions independently checked:25 core numeric,2 visible debt gaps,6 additional SEC numeric plus original HTTP evidence. First workspace helper failure is retained; corrected status/reason_code handling, exact date/unit/ID/set checks, and8 tamper negatives do not rewrite old evidence. EPS mixed-source workspace remains typed unavailable and explicitly out of the AC7 core numeric scope; see new BACKLOG item.
- Browser case1 saved only current Revenue806 evidence as revision2, reloaded and reopened exact806/publication454/raw60682/authority3933. Old revision1 still opens613/publication262/raw34522/authority2166 marked superseded historical, not replacement. Thesis/decision/valuation remain unrecorded. Annual debt gaps and document calculation warning are visible; EPS is exposed as a nonnumeric reconciliation state in full-details page8.
- New report: [stable-clock acceptance](../reports/2026-09-12_mco-plan-a-stable-acceptance.md). Closing gate and final budget/verdict are pending there until actual completion; earlier clock-failure reports remain historical.
- Final ordered gate completed:2941 backend tests passed (2 dependency warnings,1173.15s),263 frontend tests passed,lint andproduction build/TypeScript/27 pages exit0. Both worktrees diff-check clean. Final readonly acceptance824facts/40runs,original23/10; CI publicfacts/users/runs0 andno pytestschemas. Budgetfinal596,767,270bytes;76 forward-moving clock/budget samples,largest699,368,415bytes. The CI migration-only database is retained, sampler intentionally stopped. **Approved Plan A minimum acceptance is complete**, with explicit documented data/EPS limits and no commit/push/merge/deploy. See stable-clock report for exact environment, artifacts and restrictions.

## Delivery review continuation (2026-09-12)

The user requested delivery closure. Final review found and fixed one further diagnostic-retention edge case; consequently the preceding completion record applies only to its historical tree. See [delivery preparation and current gate status](2026-09-12_mco-plan-a-delivery.md) and [independent final review prompt](2026-09-12_mco-plan-a-final-review-prompt.md). A new complete gate is required for the corrected tree. No commit, push, PR, merge or deployment is authorized by this record.

Following the user's clock-adjustment confirmation, the corrected tree passed the uninterrupted delivery2 gate:2945 backend tests,263 frontend tests,build/start,migration,lint andproduction build all passed.93 database-time samples had no backward step; peak sampled cumulative833,940,025bytes stayed below the unchanged2GiB cap. Original23/10 andacceptance824/40 counts were unchanged. The task-only API was restarted after testing; read-only browser checks reopened Revenue806's original SEC evidence and the persistent document calculation warning without saving/replaying. Local delivery preparation is complete; user acceptance, actual remote CI and explicit commit/push/PR authorization remain separate. The earlier clock-failure record is retained in the linked delivery document.
