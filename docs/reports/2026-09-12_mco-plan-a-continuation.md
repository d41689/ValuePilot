# MCO A 方案：接受修订后的实施记录

## 当前结论：实现已推进，完整交付仍未完成

用户已明确接受：保留27个核心位置，要求25项准确数字及证据，FY2022/2023流动长期债务保留2项显式缺口；修复FY2020空白年度列回归；普通来源对账受阻只让可选计算不可用，不回滚合法基础事实。比较期余额发布支持不在本次范围。

这些修改已在独立修复工作树实现，并与已提交S2集成。**当前不能称为可合并或完成验收**：执行期间系统时间从约03:33 UTC回退到03:05 UTC，早于已有解析/发布记录；完整后端门禁出现失败，真实发布与Value Line重解析闭环未完成。

## 已实现并验证的行为

- SEC v2.12：仅在真实、相邻、无冲突span属性的年度空白单元格得到证明时，跳过该不完整行的上一年度推断；仍要求其他完整行证明上一财年。不补零、不改原值、不改变旧parser语义或比较期余额权限。
- 数据库新迁移继承既有lineage检查，增加空白证据的版本、报告、行列、日期、哈希与raw候选缺失检查；保留v2.11历史并拒绝不安全降级。
- 上传/重解析：仅捕获经过allowlist分类的普通对账阻断；合法基础extraction/facts保留，受阻计算不发布。真实解析、数据库、执行、权限/完整性和未知错误继续回滚；共享guard、来源优先级及旧分数读取规则未改。
- 当前文档列表和重解析响应只提供受控诊断，不泄露普通notes，不跨用户；cursor快照不附加当前诊断。上传界面、文档列表及侧栏说明不再承诺“解析成功就立即可用于筛选”。
- 计算警告按文档作有界汇总，避免多页文档重新打开后丢失早期警告。

两项独立复核的真实问题均已修复：65页警告丢失，以及重复colspan/rowspan被HTML库折叠后误入空白列例外。原始重复属性反例由发现者独立复测关闭。

## 真实文件结果，不夸大覆盖

root亲自重跑10份MCO＋3份AAPL的只读候选回归，全部断言通过。旧occurrence（仅忽略新增空白metadata）、拒绝集合和候选范围不变；FY2016/2017/2020按同一已证明规则恢复，FY2018仍因invalid_label_arc失败；其他MCO及AAPL的财年锚点保持。

脚本和完整输出分别保存在本地 `storage/mco_plan_a/retained_v212_no_write.py`、`retained-v212-no-write-root.log`。原始文件和业务数据库只读；这不是AAPL持久发布或浏览器验收。

v2.12已在本次验收克隆正常离线生成10个新parse runs、26,160个raw facts：**9/10份申报成功**，FY2018失败保持可见；零新增filings/artifacts，零外部SEC请求。Operation为 `e44717a8-d66c-49d1-b087-f4942031281d`。cutoff下尚无FY2025年报仍是选择缺口，不是解析失败。

随后publication在来源选择断言处停止：当前时间已退回03:05，最新解析记录为03:31:36。**未新增canonical facts，总数仍275**。不能把上述raw解析结果直接接入产品或沿用旧v2.11的25/27当作新版本验收。

原Value Line document1首次重解析也遭遇时间回退，数据库拒绝报告身份授权并回滚，保留失败产物 `vl-reparse-approved.json`。代码层和隔离schema通过不等于这份真实文档已经修复成功。

## 验证证据

- v2.12纯解析：164 passed；新版本SQL聚焦：12 passed。此前版本/迁移/历史请求重放组25 passed，补充旧版本/合并/反号组6 passed。
- 新上传/API组合：16 passed；多页汇总后专属上传文件15 passed；前端结果提示4 passed。
- 独立重复属性复验：8例通过。
- 时间回退后的全新schema中，Piotroski严格manifest专项45 passed；不把这当作完整门禁，也不据此认定全部失败均由时钟造成。
- 本轮完整门禁：隔离compose构建和迁移通过；完整后端 **2793 passed /148 failed /2 warnings，1023.95秒**。第一项失败直接显示03:03的cutoff早于currentness authority。随后不改后端代码，在新建隔离schema中对这148个失败节点逐项复跑，**148 passed /2 warnings，182.74秒**；这支持时间回退影响的判断，但不替代稳定时钟下的完整门禁，也未排除整套测试的顺序依赖。
- 前端完整glob **263 passed**；lint初次因新增CommonJS测试文件缺少仓库惯用声明失败，修正后完整glob和lint均通过；production build退出0，TypeScript及27个静态页通过。上一轮v2.11的2883/259不能代替本轮。
- 后端最终工具输出过大被截断，保留为 `backend-full-v212-excerpt.log`，明确不是完整原始日志；全部148个失败节点独立保存在 `backend-full-v212-failed-nodes.json`。不把摘要或截断日志描述为完整stdout。
- 一轮聚焦测试被root自己的CI容器重建中断（exit137），未计为通过；仅清理了该次遗留的准确独占schema `valuepilot_pytest_4848c79405c1`，未删除public业务数据。

## 基线、数据边界和剩余条件

修复：`codex/mco-plan-a`，基线 `15f4ba02944a6fe29962136665d9e906010c422a`。集成：已提交S2 `1065f692a3b1453fa221c1da42cedbc4ab85a9fb`＋修复，不包含原用户工作区未提交的economics修改。原工作区未改；本次没有commit、push、merge、deploy或数据提升。

只向新建验收库 `valuepilot_acceptance_mcoplana` 和专用CI库/schema写入。原MCO实验库保持23 facts/10 runs。预算继续沿用原始3,735,552-byte基线，包含整个CI数据库和文件。测试后观察到的最大检查点为 **2,079,182,012 bytes**，低于2GiB；未作连续瞬时峰值监控，不能宣称已独立证明测试期间每个瞬间均未超限。为释放本次临时测试占用，先核实CI库public facts/users/parse runs均0、无遗留pytest schema或活动连接，再清理本次新建的准确CI库；原始预算基线不重置。

清理后只读复核：该CI库已不存在，原实验库仍23 facts/10 runs，验收库仍275 facts/40 runs；最新占用检查点 **497,350,854 bytes**，原始基线仍为3,735,552。删除的是本次可重建的测试库及其迁移/系统目录数据，不是保留验收或用户业务数据；报告和测试结果保留。下次门禁必须先重建该专用CI库，再运行迁移；不能直接把停止的CI服务当作就绪环境。

待时间稳定并超过已有记录后：

1. 重跑完整六条门禁并逐项处理可复现失败。
2. 正常v2.12 publication＋同请求重放；按冻结矩阵和批准补充合同验证25数字、2缺口、6附加项。
3. 正常Value Line重解析，核实24项收入/每股收入、actual/estimate、身份和计算警告。
4. 认证HTTP与浏览器中核对读取、证据、旧引用重新打开；不替用户填写thesis或作投资结论。
5. 最终只读计数/预算检查，再作交付判定。

FY2022 EPS的0.73未解释残差、比较期债务权限、FY2018标签失败以及VL早年/预测范围，均保持显式限制，见BACKLOG。没有引入AI事实写入或自动调整来消除差异。

此前误向旧测试库创建的专用账户与refresh token仍待清理许可；本次“接受”不包含该许可，未擅自删除。详见[完整任务记录](../tasks/2026-09-12_mco-plan-a.md)。
